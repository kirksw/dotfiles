#include <iostream>
using namespace std;

// Struct members are all public by default

enum Colour { red, green, blue }; // Normally in header file

// Defines structure apple
struct apple {
    float weight;
    Colour c;
};

// Instantiates structure as orange
struct {
    float weight;
    Colour c;
} orange;

apple createApple()
{
    apple myApple;
    myApple.weight = 75.5; // grams
    myApple.c = red;    

    return myApple;
}

int main() {
    apple firstApple = createApple();

    cout << "Apple weight: " << firstApple.weight;
}
